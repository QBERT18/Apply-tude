# Local development MongoDB connection (used when NODE_ENV !== "production")
MONGODB_URI_DEV=mongodb://localhost:27017/applytude

# Production MongoDB connection (used when NODE_ENV === "production").
# On Vercel, set this in Settings -> Environment Variables (Production scope).
MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/?appName=YOUR_APP
